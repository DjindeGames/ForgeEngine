#pragma once

#include <vector>

namespace ForgeEngine
{
    /*
    class MeshTriangle
    {
    public:
        MeshTriangle(const Vector3[3]& vertices)
        {
            std::copy(std::begin(vertices), std::end(vertices), std::begin(m_Vertices));
        }
    private:
        Vector3 m_Vertices[3];
    };

    class Mesh
    {
    public:
        Mesh(const std::vector<Vector3>& vertices, const std::vector<unsigned int>& triangleIndices);

        const std::vector<Vector3>& GetVertices() const { return m_Vertices; }
        const std::vector<unsigned int>& GetVertices() const { return m_Vertices; }

    private:
        std::vector<Vector3> m_Vertices;
        std::vector<unsigned int> m_TrianglesIndices
    };
    */
}